#include <iostream>
#include <stdio>

using namespace std;

int main() {
  int nodeAmount, rootId, edgesAmount;
  cin >> nodeAmount >> rootId >> edgesAmount;
  for(int i = 0; i < edgesAmount; i++) {
    int p, c;
    cin >> p >> c;
  }
  return 0;
}